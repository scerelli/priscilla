# Identicon

Simple Identicon generator made while learning elixir.
`Identicon` main function accepts a string and returns a png image that is an identicon of the given string.

## Installation

`Identicon.main("string")`

